{% extends '::base.html.twig' %}

{% block body -%}
    
    <div class="panel panel-info">  
    
        <div class="panel-body text-center" >
            <img src="{{asset('images/Encabezado_reportes.png')}}">
                <p>Reporte plazas Remuneradas y Ad-Honorem</p>
                <br />
                <br />
            <table class="table table-striped table-hover table-condensed" border="1">
              <thead class="table-param">
                <tr align="center">
                    <th class="table-header" rowspan="2" align="center" valign="top"><center>Universidad</center></th><br>
            <th class="table-header" colspan="2" align="center"><center>Medicina</center></th>
            ></th> 
               </tr>
                <tr>
                    
                    <td align="center"> Plazas Remuneradas</td>
                    <td align="center"> Plazas Ad-Honorem</td>
                
                    
                    
                </tr>    
              </thead> 
              <tbody class="table-param">
               {%for univs in universidad%}
               <tr> 
                   <th>{{univs.nombre}}</th>
                   
                    {% set count = 0%} <!--Contador para que se vean las columnas -->
                                {%for res in resultado if (res.id == univs.id)%}                                    
                                    {% if (res.cuenta != 0) %} <!-- Si el valor es distinto de 0, se escribe de lo contrario se imprime una columna vacia -->
                                        <th><center> {{res.cuenta}}</center> </th>
                                    {%else %}
                                        <th> </th> 
                                    {%endif %}
                                    {% set count = count +1 %}
                                {% endfor %}    
              
                     {%for res1 in resultado1 if (res1.id == univs.id)%}                                    
                                    {% if (res1.cuenta != 0) %} <!-- Si el valor es distinto de 0, se escribe de lo contrario se imprime una columna vacia -->
                                        <th><center> {{res1.cuenta}}</center> </th>
                                    {%else %}
                                        <th> </th> 
                                    {%endif %}
                                    {% set count = count +1 %}
                                {% endfor %}   
              
           
                                {%set resta = 4-count%}
                                {% for i in 1..resta %}
                                    <th> </th>
                                {% endfor%}
                   
                   
                   
                    
               </tr>       
               {% endfor%}
               
                 
            </tbody>  
            </table>
            <a href="{{url('minsal_si_gminsal_sig_plazas')}}"> Regresar </a>          
                <br />
                <br />     
        </div>          
    </div>    
{% endblock %} {# empty Twig template #}
